import { type ReactElement } from 'react';
import { Title, Text } from '@mantine/core';

export default function TprLab2(): ReactElement {
  return (
    <>
      <Title order={3} mb="sm">
        2. Вибір рішення в умовах ризику
      </Title>
      <Text c="dimmed">Lab 2 content placeholder. Replace with actual lab implementation.</Text>
    </>
  );
}
